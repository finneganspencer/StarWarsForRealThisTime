@using Microsoft.AspNet.Identity
@*@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()

    <ul class="nav navbar-nav navbar-right">
        <li>
            @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
        </li>
        <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
    </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Register", "Register", "Home", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}*@

<ul class="nav navbar-nav navbar-right">
    @*<li>@Html.ActionLink("Hello " + StarWarsWebsite.Controllers.GlobalVariables.currentUserName + "!", "Index", "Home", routeValues: null, htmlAttributes: new { title = "Manage" })</li>*@
    <li>
        @if (StarWarsWebsite.Controllers.GlobalVariables.currentUserID != 0)
        {
            @Html.ActionLink("Hello " + StarWarsWebsite.Controllers.GlobalVariables.currentUserName + "!", "Edit", "Users", new { id = StarWarsWebsite.Controllers.GlobalVariables.currentUserID }, htmlAttributes: new { title = "Manage" })
        }

        else
        {
            @Html.ActionLink("Hello " + StarWarsWebsite.Controllers.GlobalVariables.currentUserName + "!", "Index", "Home", null, htmlAttributes: new { title = "Manage" })
        }

    </li>
    <li>@Html.ActionLink("Register", "Create", "Users", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
    @if (StarWarsWebsite.Controllers.GlobalVariables.currentUserName != "New User")
    {
        <li>@Html.ActionLink("Log Out", "Logout", "Users", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    }
    else
    {
        <li>@Html.ActionLink("Log in", "Login", "Users", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    }
</ul>
